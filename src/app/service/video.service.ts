import { Injectable } from '@angular/core';
import { Subject } from 'rxjs';

@Injectable()
export class VideoService {

    constructor(
    ) {}

    videoToImg(): string {
        return '';
    }
}
